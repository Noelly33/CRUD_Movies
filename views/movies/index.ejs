<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard de Películas</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 h-screen">
<div class="flex h-full">
    <!-- Sidebar -->
    <div class="w-1/5 bg-gray-800 text-white flex flex-col">
        <div class="py-6 px-4 text-center text-xl font-bold border-b border-gray-700">Dashboard</div>
        <nav class="mt-6">
            <a href="#" class="block py-3 px-4 hover:bg-gray-700">Inicio</a>
            <a href="#" class="block py-3 px-4 hover:bg-gray-700">Películas</a>
            <a href="#" class="block py-3 px-4 hover:bg-gray-700">Ajustes</a>
        </nav>
    </div>

    <!-- Main Content -->
    <div class="flex-1 bg-gray-100 p-6">
        <header class="flex justify-between items-center mb-6">
            <h1 class="text-3xl font-bold">Películas</h1>
            <a href="/movies/create" class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded">Agregar Película</a>
        </header>

        <!-- Table -->
        <div class="overflow-x-auto">
            <table class="table-auto w-full bg-white rounded-lg shadow-md">
                <thead>
                    <tr class="bg-gray-200">
                        <th class="border px-4 py-2">ID</th>
                        <th class="border px-4 py-2">Título</th>
                        <th class="border px-4 py-2">Género</th>
                        <th class="border px-4 py-2">Año</th>
                        <th class="border px-4 py-2">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (movies && movies.length > 0) { %>
                        <% movies.forEach(movie => { %>
                            <tr class="hover:bg-gray-100">
                                <td class="border px-4 py-2 text-center"><%= movie.id %></td>
                                <td class="border px-4 py-2"><%= movie.title %></td>
                                <td class="border px-4 py-2"><%= movie.genre %></td>
                                <td class="border px-4 py-2 text-center"><%= movie.year %></td>
                                <td class="border px-4 py-2 text-center">
                                    <a href="/movies/edit/<%= movie.id %>" class="text-blue-500 hover:underline">Editar</a> |
                                    <a href="/movies/delete/<%= movie.id %>" class="text-red-500 hover:underline">Eliminar</a>
                                </td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="5" class="border px-4 py-2 text-center text-gray-500">No hay películas disponibles.</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</div>
</body>
</html>
